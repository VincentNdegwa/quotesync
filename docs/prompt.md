That looks nice but the target is not Africa only so we should not mention geo-location to avoid limiting our selves , let the target be global , so update that, also update features we have bettter real features instead of faking say the good features we have , like currently we don't have mpesa, so you can find other features, also the, currency should not be mentioned as KES since quotesync the currency is dynamic, also improve the UI, it currenctly looks good, but most of SAAS looks like this so if possible you can redesign, have a new layout, unique, unique feel, the UI does not look AI generated, i mean put some creativity on this, but i like it so far we just need to remove the generic design and have a quotesync brand feel